# Claude Accessibility Skills

These skills are set up such that the primary `accessibiility-audit` skill runs as an orchestrator, checking the page for which tests to run, at which point it will dispatch other skills as needed. The `keyboard-dropdown-audit` skill is a more in-depth test to check dropdowns for keyboard accessibility. 